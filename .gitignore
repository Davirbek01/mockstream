*.rar
*.zip
*.7z
*.tar.gz
.env
.venv/
__pycache__/
*.pyc
*.py
# exception: the OG cover generator is repo tooling, not a scratch script
!tools/og-covers/generate.py
desktop.ini

# Local SQL migrations that contain secrets (service role key, etc.)
site/supabase-cleanup-fix.sql

# Vite pilot — local dev tool only, never deploy
vite-app/

# React + Vite pilot — local only, never deploy
react-app/

supabase/.temp/

# Local design previews — rendered from real mocks for review, never deployed.
site/_proto/
